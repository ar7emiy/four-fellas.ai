# historical_shorts_v1 (archived)

Original pre-pivot codebase. Generated short-form historical drama videos from Wikipedia traversal + Claude scriptwriting + ffmpeg + multi-platform distribution (YouTube, Meta, TikTok).

**Why archived:** Project pivoted from historical content to AI influencer pipeline. Code retained for reference (Wikipedia traversal, Claude prompt patterns, ffmpeg pan/zoom, OAuth flows for distribution platforms may be useful later).

**Entry point:** `historical_shorts.py` (single 622-line script).

**Do not run from this directory** — dependencies are listed in the local `requirements.txt` but the active project uses a different stack. See repo root `README.md`.
